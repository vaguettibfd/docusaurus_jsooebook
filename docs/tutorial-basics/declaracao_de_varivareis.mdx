# Declaração de Variáveis

O código abaixo demonstra a criação de variáves JavaScript de várias maneiras, após observar o código e os comentários de linha tente manipulá-lo através do link indicativo.

```js
var x = 6;
    console.log(x);

    const y = "a";  // definição de variável const
    // y = "b";
    console.log(y);
    // VARIAVEIS GLOBAIS E LOCAIS
    {   // bloco de códigos
       let z = 4;
       console.log(x);
       console.log(y);
       console.log(z);  
    }

//console.log(z);  <-- Essa linha, se descomentada, gera erro pois local ao bloco acima 
```

## 🧪 Teste o código no StackBlitz

👉 **[Executar este exemplo no StackBlitz](https://stackblitz.com/~/github.com/vaguettibfd/BFD_JS_OO?file=declvar.js)**

> 💡 Experimente modificar o código e observe o comportamento das variáveis.


## Dicas do professor

> bla, blá ...